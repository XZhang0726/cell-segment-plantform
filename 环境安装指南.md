# 细胞分割项目环境安装指南

## 概述

本项目包含三个深度学习细胞分割模型：
- **Cellpose**: 通用细胞分割（细胞质和细胞核）
- **CellSAM**: 基于 Segment Anything Model 的通用对象分割
- **CellViT**: 专注于病理切片细胞核分割

## 环境要求

### 硬件要求
- **GPU**: NVIDIA GPU with CUDA support (推荐 8GB+ VRAM)
- **内存**: 至少 16GB RAM
- **存储**: 至少 10GB 可用空间（用于模型文件）

### 软件要求
- **操作系统**: Windows 10/11, Linux, macOS
- **Python**: 3.12.x
- **CUDA**: 12.8 (如果使用 GPU)
- **Conda**: Anaconda 或 Miniconda

## 环境配置

### 方案 1: 使用 Conda 创建环境（推荐）

#### 1. 创建 cellpose_gpu 环境

```bash
# 创建 Python 3.12 环境
conda create -n cellpose_gpu python=3.12 -y

# 激活环境
conda activate cellpose_gpu
```

#### 2. 安装 PyTorch (CUDA 12.8)

```bash
# 安装 PyTorch with CUDA 12.8
pip install torch==2.10.0+cu128 torchvision==0.25.0+cu128 torchaudio==2.10.0+cu128 --index-url https://download.pytorch.org/whl/cu128
```

#### 3. 安装核心依赖

```bash
# 安装 numpy (重要: 必须是 1.26.4)
pip install numpy==1.26.4

# 安装 Cellpose
pip install cellpose==3.1.0

# 安装 Segment Anything
pip install segment-anything==1.0

# 安装 Streamlit
pip install streamlit==1.53.1
```

#### 4. 安装其他依赖

```bash
# 安装图像处理库
pip install opencv-python==4.10.0 pillow==10.4.0 scikit-image imageio tifffile

# 安装科学计算库
pip install scipy pandas matplotlib plotly

# 安装工具库
pip install loguru tqdm pyyaml
```

### 方案 2: 使用 requirements.txt 快速安装

```bash
# 激活环境
conda activate cellpose_gpu

# 先安装 PyTorch (CUDA 版本)
pip install torch==2.10.0+cu128 torchvision==0.25.0+cu128 torchaudio==2.10.0+cu128 --index-url https://download.pytorch.org/whl/cu128

# 安装其他依赖
pip install -r requirements_complete.txt
```

**注意**: 由于 PyTorch CUDA 版本需要特殊的索引 URL，建议先单独安装 PyTorch，再安装其他依赖。

## CellViT 专用环境配置

**重要**: CellViT 需要在专用环境中运行，因为它依赖 numpy 1.26.4，与主环境可能存在版本冲突。

### 创建 CellViT 环境

```bash
# 创建专用环境
conda create -p ./env_cellvit python=3.12 -y

# 激活环境
source activate ./env_cellvit  # Linux/Mac
# 或
conda activate ./env_cellvit  # Windows

# 安装 CellViT
pip install cellvit==1.0.9

# 安装其他依赖
pip install numpy==1.26.4 torch torchvision opencv-python-headless streamlit
```

### 使用 CellViT

```bash
# 激活 CellViT 环境
source activate ./env_cellvit

# 启动应用
streamlit run app_enhanced.py
```

## 模型文件下载

### CellSAM 模型文件

CellSAM 需要预训练的 SAM 模型检查点文件。请下载并放置在 `models/sam/` 目录下：

```bash
# 创建模型目录
mkdir -p models/sam

# 下载 vit_b 模型 (358MB) - 推荐首次使用
curl -L https://dl.fbaipublicfiles.com/segment_anything/sam_vit_b_01ec64.pth -o models/sam/sam_vit_b_01ec64.pth

# 下载 vit_l 模型 (1.2GB) - 平衡速度和精度
curl -L https://dl.fbaipublicfiles.com/segment_anything/sam_vit_l_0b3195.pth -o models/sam/sam_vit_l_0b3195.pth

# 下载 vit_h 模型 (2.4GB) - 最高精度
curl -L https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth -o models/sam/sam_vit_h_4b8939.pth
```

### Cellpose 和 CellViT 模型

Cellpose 和 CellViT 的模型会在首次运行时自动下载，无需手动下载。

## 验证安装

### 验证 cellpose_gpu 环境

```bash
# 激活环境
conda activate cellpose_gpu

# 验证 Python 版本
python --version  # 应显示 Python 3.12.x

# 验证 numpy 版本
python -c "import numpy; print(f'NumPy: {numpy.__version__}')"  # 应显示 1.26.4

# 验证 PyTorch 和 CUDA
python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA available: {torch.cuda.is_available()}')"

# 验证 Cellpose
python -c "import cellpose; print('Cellpose: OK')"

# 验证 Segment Anything
python -c "import segment_anything; print('Segment Anything: OK')"

# 验证 Streamlit
streamlit --version
```

### 验证 CellViT 环境

```bash
# 激活环境
source activate ./env_cellvit

# 验证 CellViT
python -c "import cellvit; print('CellViT: OK')"
```

## 常见问题

### Q1: CUDA out of memory 错误

**原因**: GPU 内存不足

**解决方案**:
- 使用 CPU 模式（在应用中取消勾选"使用GPU加速"）
- 使用更小的模型（如 CellSAM 使用 vit_b 代替 vit_h）
- 减小批处理大小
- 关闭其他占用 GPU 的程序

### Q2: numpy 版本冲突

**现象**: `ImportError: numpy.core.multiarray failed to import`

**解决方案**:
```bash
# 重新安装 numpy 1.26.4
pip uninstall numpy -y
pip install numpy==1.26.4
```

### Q3: PyTorch 无法使用 CUDA

**现象**: `torch.cuda.is_available()` 返回 False

**解决方案**:
- 确认已安装 CUDA 12.8
- 重新安装 PyTorch CUDA 版本：
```bash
pip uninstall torch torchvision torchaudio -y
pip install torch==2.10.0+cu128 torchvision==0.25.0+cu128 torchaudio==2.10.0+cu128 --index-url https://download.pytorch.org/whl/cu128
```

### Q4: CellViT 导入错误

**现象**: `ImportError: CellViT not available`

**原因**: 未在 CellViT 专用环境中运行

**解决方案**:
```bash
source activate ./env_cellvit
streamlit run app_enhanced.py
```

## 启动应用

### 使用 Cellpose 或 CellSAM

```bash
# 激活 cellpose_gpu 环境
conda activate cellpose_gpu

# 启动应用
streamlit run app_enhanced.py
```

### 使用 CellViT

```bash
# 激活 CellViT 环境
source activate ./env_cellvit

# 启动应用
streamlit run app_enhanced.py
```

## 总结

- **cellpose_gpu 环境**: 用于 Cellpose 和 CellSAM，numpy 1.26.4
- **env_cellvit 环境**: 专用于 CellViT，numpy 1.26.4
- **模型文件**: CellSAM 需要手动下载，其他模型自动下载
- **GPU 支持**: 推荐使用 CUDA 12.8 的 GPU 加速

## 参考资料

- [Cellpose GitHub](https://github.com/MouseLand/cellpose)
- [Segment Anything GitHub](https://github.com/facebookresearch/segment-anything)
- [CellViT GitHub](https://github.com/TIO-IKIM/CellViT)
- [PyTorch 安装指南](https://pytorch.org/get-started/locally/)
