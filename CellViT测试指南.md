# CellViT 功能测试指南

## 测试前准备

### 1. 激活 CellViT 环境

```bash
cd /c/Users/XB001/Desktop/cc_works/xibaofenge
source activate ./env_cellvit
```

### 2. 验证环境

```bash
python -c "import cellvit; import numpy as np; print(f'CellViT: OK, NumPy: {np.__version__}')"
```

预期输出：`CellViT: OK, NumPy: 1.26.4`

## 启动应用

```bash
streamlit run app_enhanced.py
```

## 测试步骤

### 测试 1: 单图模式测试

1. **上传图像**
   - 点击"上传细胞图像"
   - 选择一张测试图像（推荐 256x256 或更小）

2. **选择 CellViT 方法**
   - 在"分割方法"下拉菜单中选择"CellViT深度学习"
   - 检查环境状态：
     - ✅ 绿色提示：环境正确
     - ⚠️ 红色警告：环境未激活

3. **配置参数**
   - 模型类型：CellViT-256（默认）
   - 目标图像大小：256（推荐）
   - GPU 加速：根据硬件选择

4. **执行分割**
   - 点击"开始分割"按钮
   - 观察进度条
   - 查看分割结果

5. **验证结果**
   - 检查分割掩码是否正确显示
   - 查看检测到的细胞数量
   - 对比原图和分割结果

### 测试 2: 批处理模式测试

1. **切换到批处理模式**
   - 点击"📦 批量处理"标签页

2. **上传多张图像**
   - 上传 2-5 张测试图像

3. **选择 CellViT 方法**
   - 在"分割方法"下拉菜单中选择"CellViT深度学习"
   - 检查环境状态

4. **配置参数**
   - 设置相同的参数

5. **批量处理**
   - 点击"🚀 批量处理"按钮
   - 观察处理进度
   - 查看批处理结果

### 测试 3: 性能测试

1. **CPU vs GPU 对比**
   - 使用相同图像
   - 分别测试 CPU 和 GPU 模式
   - 记录处理时间

2. **不同图像大小**
   - 测试小图像（128x128）
   - 测试中等图像（256x256）
   - 测试大图像（512x512）

## 预期结果

### 正常情况

- ✅ 环境检测显示绿色成功提示
- ✅ 模型加载成功（首次需要下载，约 500MB）
- ✅ 分割结果显示细胞核轮廓
- ✅ 检测到的细胞数量合理
- ✅ GPU 模式比 CPU 模式快 5-10 倍

### 异常情况处理

#### 环境未激活
**现象**：红色警告"CellViT环境未激活"

**解决**：
```bash
source activate ./env_cellvit
streamlit run app_enhanced.py
```

#### 模型下载失败
**现象**：错误提示"模型下载失败"

**解决**：
- 检查网络连接
- 重试或使用代理

#### GPU 内存不足
**现象**：CUDA out of memory

**解决**：
- 取消勾选"使用GPU加速"
- 使用 CPU 模式

#### 分割结果异常
**现象**：检测到 0 个细胞或结果不合理

**可能原因**：
- 图像质量问题
- 图像类型不适合（CellViT 主要用于细胞核）
- 尝试调整目标图像大小

## 性能基准

### 参考数据（RTX 5070）

| 图像大小 | GPU 模式 | CPU 模式 |
|---------|---------|---------|
| 256x256 | ~2秒    | ~15秒   |
| 512x512 | ~5秒    | ~45秒   |
| 1024x1024 | ~15秒  | ~180秒  |

## 测试检查清单

- [ ] 环境激活成功
- [ ] 应用启动正常
- [ ] 单图模式分割成功
- [ ] 批处理模式运行正常
- [ ] GPU 加速工作正常
- [ ] 结果可视化正确
- [ ] 特征提取功能正常
- [ ] 导出功能正常

## 常见问题

### Q: 为什么必须在 cellvit 环境中运行？
A: CellViT 需要 numpy 1.26.4，而主项目使用 numpy 2.x，版本不兼容。

### Q: 可以在 cellpose_gpu 环境中使用 CellViT 吗？
A: 不可以。必须在 cellvit 环境中运行，否则会出现导入错误。

### Q: CellViT 和 Cellpose 有什么区别？
A:
- CellViT：专注于细胞核分割，基于 Vision Transformer
- Cellpose：通用细胞分割，支持细胞质和细胞核

### Q: 如何选择合适的模型？
A:
- 细胞核分割：CellViT 或 Cellpose (nuclei)
- 细胞质分割：Cellpose (cyto2)
- 通用场景：Cellpose (cyto2)

## 报告问题

如果遇到问题，请提供：
1. 错误信息截图
2. 环境信息（numpy 版本）
3. 测试图像（如果可能）
4. 操作步骤

## 下一步

测试完成后，可以：
1. 添加第 2 个深度学习模型
2. 优化 CellViT 性能
3. 添加更多预训练模型选项
