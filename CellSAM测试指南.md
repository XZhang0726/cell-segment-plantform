# CellSAM 功能测试指南

## 测试前准备

### 1. 确认环境

CellSAM 直接在 cellpose_gpu 环境中运行，无需创建专用环境：

```bash
cd /c/Users/XB001/Desktop/cc_works/xibaofenge

# 激活 cellpose_gpu 环境
source activate cellpose_gpu

# 验证依赖已安装
python -c "import segment_anything; import torch; print('✓ CellSAM dependencies ready')"
```

预期输出：`✓ CellSAM dependencies ready`

### 2. 下载模型文件

```bash
# 创建模型目录
mkdir -p models/sam

# 下载 vit_b 模型（推荐用于测试）
wget https://dl.fbaipublicfiles.com/segment_anything/sam_vit_b_01ec64.pth -P models/sam/
```

**注意**: 首次测试建议只下载 vit_b 模型（91MB），后续可根据需要下载其他模型。

## 启动应用

```bash
# 在 cellpose_gpu 环境中启动
source activate cellpose_gpu
streamlit run app_enhanced.py
```

## 测试步骤

### 测试 1: 单图模式测试

1. **上传图像**
   - 点击"上传细胞图像"
   - 选择一张测试图像（任意大小，推荐 512x512 或更小）

2. **选择 CellSAM 方法**
   - 在"分割方法"下拉菜单中选择"CellSAM深度学习"
   - 查看提示信息：应显示"CellSAM直接在当前环境中运行"

3. **配置参数**
   - 模型类型：vit_b（推荐用于测试）
   - 提示点密度：32（默认）
   - GPU 加速：根据硬件选择

4. **执行分割**
   - 点击"开始分割"按钮
   - 观察进度条
   - 查看分割结果

5. **验证结果**
   - 检查分割掩码是否正确显示
   - 查看检测到的细胞数量
   - 对比原图和分割结果

### 测试 2: 批处理模式测试

1. **切换到批处理模式**
   - 点击"📦 批量处理"标签页

2. **上传多张图像**
   - 上传 2-5 张测试图像

3. **选择 CellSAM 方法**
   - 在"分割方法"下拉菜单中选择"CellSAM深度学习"
   - 查看提示信息

4. **配置参数**
   - 设置相同的参数

5. **批量处理**
   - 点击"🚀 批量处理"按钮
   - 观察处理进度
   - 查看批处理结果

### 测试 3: 不同模型对比测试

1. **测试 vit_b 模型**
   - 模型类型：vit_b
   - 记录处理时间和检测结果

2. **测试 vit_l 模型**（如果已下载）
   - 模型类型：vit_l
   - 对比 vit_b 的结果

3. **测试 vit_h 模型**（如果已下载）
   - 模型类型：vit_h
   - 对比前两个模型的结果

### 测试 4: 参数调优测试

1. **提示点密度测试**
   - 测试 points_per_side = 16（快速，可能漏检）
   - 测试 points_per_side = 32（平衡）
   - 测试 points_per_side = 48（精细，较慢）

2. **观察结果差异**
   - 检测到的细胞数量
   - 处理时间
   - 分割质量

## 预期结果

### 正常情况

- ✅ 依赖验证显示成功提示
- ✅ 模型加载成功（首次需要下载模型文件）
- ✅ 分割结果显示细胞轮廓
- ✅ 检测到的细胞数量合理
- ✅ GPU 模式比 CPU 模式快 3-5 倍
- ✅ 模型缓存机制工作正常（第二次运行更快）

### 异常情况处理

#### 模型文件未找到
**现象**：错误提示"Model checkpoint not found"

**解决**：
```bash
# 下载对应的模型文件
wget https://dl.fbaipublicfiles.com/segment_anything/sam_vit_b_01ec64.pth -P models/sam/
```

#### GPU 内存不足
**现象**：CUDA out of memory

**解决**：
- 使用 CPU 模式：取消勾选"使用GPU加速"
- 使用更小的模型：vit_b 代替 vit_h
- 减小提示点密度：points_per_side=16

#### 检测到的细胞太少
**现象**：检测到的细胞数量明显少于实际

**解决**：
- 增加提示点密度：points_per_side=48 或 64
- 使用更大的模型：vit_l 或 vit_h
- 检查图像质量和对比度

#### 检测到太多碎片
**现象**：检测到很多小碎片或噪声

**解决**：
- 减小提示点密度：points_per_side=16 或 24
- 在预处理中启用去噪选项
- 调整图像对比度

## 性能基准

### 参考数据（RTX 5070）

#### vit_b 模型
| 图像大小 | GPU 模式 | CPU 模式 |
|---------|---------|---------|
| 256x256 | ~3秒    | ~20秒   |
| 512x512 | ~6秒    | ~60秒   |
| 1024x1024 | ~18秒  | ~240秒  |

#### vit_l 模型
| 图像大小 | GPU 模式 | CPU 模式 |
|---------|---------|---------|
| 256x256 | ~5秒    | ~40秒   |
| 512x512 | ~12秒   | ~120秒  |
| 1024x1024 | ~35秒  | ~480秒  |

## 测试检查清单

- [ ] cellpose_gpu 环境激活成功
- [ ] 依赖验证通过
- [ ] 模型文件下载完成
- [ ] 应用启动正常
- [ ] 单图模式分割成功
- [ ] 批处理模式运行正常
- [ ] GPU 加速工作正常
- [ ] 不同模型类型测试完成
- [ ] 参数调优测试完成
- [ ] 结果可视化正确
- [ ] 特征提取功能正常
- [ ] 导出功能正常

## 常见问题

### Q: CellSAM 需要在专用环境中运行吗？
A: 不需要。CellSAM 现在直接在 cellpose_gpu 环境中运行，无需创建专用环境。

### Q: 三种模型（vit_b, vit_l, vit_h）有什么区别？
A:
- vit_b (91M): 速度最快，精度中等，适合快速预览
- vit_l (308M): 平衡速度和精度，推荐日常使用
- vit_h (636M): 精度最高，速度较慢，适合高精度需求

### Q: CellSAM 和 Cellpose/CellViT 有什么区别？
A:
- CellSAM：基于 SAM，通用对象分割，适用于多种图像类型
- CellViT：专注于细胞核分割，基于 Vision Transformer
- Cellpose：专门为细胞设计，支持细胞质和细胞核

### Q: 如何选择合适的提示点密度？
A:
- 16-24：快速处理，适合大批量或预览
- 32：默认值，平衡速度和精度
- 48-64：精细检测，适合高精度需求

### Q: 为什么第二次运行更快？
A: CellSAM 使用模型缓存机制，首次加载后模型会保存在内存中，后续使用无需重新加载。

## 报告问题

如果遇到问题，请提供：
1. 错误信息截图
2. 环境信息（Python 版本、numpy 版本）
3. 使用的模型类型和参数
4. 测试图像（如果可能）
5. 操作步骤

## 下一步

测试完成后，可以：
1. 对比 CellSAM、CellViT 和 Cellpose 的性能
2. 针对特定细胞类型优化参数
3. 探索 CellSAM 在其他图像类型上的应用
4. 下载并测试其他模型规格（vit_l, vit_h）

